#!/bin/bash

cd themes/gmd && mkdir extensions && cd extensions && touch reducers.js && cat 'export default null;' > reducers.js
cd themes/ios11 && mkdir extensions && cd extensions && touch reducers.js && cat 'export default null;' > reducers.js