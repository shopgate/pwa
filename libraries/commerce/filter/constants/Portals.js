// FEATURES
const FILTER = 'filter';

// CONTENTS
const PRICE_RANGE = 'price-range';
const SELECTOR = 'selector';

// POSITIONS
const BEFORE = 'before';
const AFTER = 'after';

export const FILTER_PRICE_RANGE = `${FILTER}.${PRICE_RANGE}`;
export const FILTER_PRICE_RANGE_BEFORE = `${FILTER}.${PRICE_RANGE}.${BEFORE}`;
export const FILTER_PRICE_RANGE_AFTER = `${FILTER}.${PRICE_RANGE}.${AFTER}`;

export const FILTER_SELECTOR = `${FILTER}.${SELECTOR}`;
export const FILTER_SELECTOR_BEFORE = `${FILTER}.${SELECTOR}.${BEFORE}`;
export const FILTER_SELECTOR_AFTER = `${FILTER}.${SELECTOR}.${AFTER}`;
