import { css } from 'glamor';

export const content = css({
  overflow: 'hidden',
  willChange: 'height',
}).toString();

export const contentInner = css({
  padding: '0 16px 16px',
  overflow: 'hidden',
}).toString();
