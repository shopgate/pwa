import { css } from 'glamor';

const form = css({
  display: 'flex',
  flexGrow: 1,
});

const field = css({
  outline: 0,
  padding: '0 16px',
  width: '100%',
});

export default {
  form,
  field,
};
