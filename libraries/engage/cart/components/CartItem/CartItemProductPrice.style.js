// @flow
import { css } from 'glamor';

export const price = css({
  fontSize: '1rem',
  fontWeight: 500,
  justifyContent: 'flex-end',
  textAlign: 'right',
}).toString();

export const priceStriked = css({
  fontSize: '.875rem',
  textAlign: 'right',
}).toString();
