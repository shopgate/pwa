// Backwards-compatible shim. The shop settings selectors now live in the
// dedicated `settings` package: @shopgate/engage/settings/selectors/shopSettings
export * from '../../settings/selectors/shopSettings';
