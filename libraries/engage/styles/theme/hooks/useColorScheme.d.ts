import { type ColorSchemeContextValue  } from '../providers/ThemeProvider';

/**
 * Returns the currently selected color scheme and a function to update it.
 */
export default function useColorScheme(): ColorSchemeContextValue;
