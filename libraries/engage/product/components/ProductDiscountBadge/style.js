import { css } from 'glamor';

const styles = {
  container: css({
    lineHeight: 1,
    position: 'absolute',
    left: 22,
    top: 17,
    width: 65,
    zIndex: 10,
  }),
  badge: css({
    fontSize: '1.15rem',
  }),
};

export default styles;
