import { css } from 'glamor';

export const root = css({
  position: 'absolute',
  height: 2,
  bottom: 0,
  width: '100%',
  transition: 'left .2s ease',
  backgroundColor: 'var(--color-primary)',
}).toString();

