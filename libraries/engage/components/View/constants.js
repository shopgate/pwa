/**
 * Duration of the opacity crossfade between routed views, in milliseconds. The
 * single source of truth: the theme-ios11 hero-transition flight is tuned to
 * match this so the flying clone lands as the new view finishes fading in.
 * @type {number}
 */
export const FADE_DURATION = 350;
