import { css } from 'glamor';

export default css({
  bottom: 2,
  color: 'var(--color-state-alert)',
  fontSize: 12,
  lineHeight: '14px',
  overflow: 'hidden',
  position: 'absolute',
  textOverflow: 'ellipsis',
  whiteSpace: 'nowrap',
  width: '100%',
}).toString();
