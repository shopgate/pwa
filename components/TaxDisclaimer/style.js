import { css } from 'glamor';
import colors from 'Styles/colors';

export default css({
  background: colors.background,
  display: 'block',
  fontSize: 12,
  padding: '20px 0',
  textAlign: 'center',
}).toString();
