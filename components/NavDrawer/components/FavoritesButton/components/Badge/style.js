import { css } from 'glamor';
import colors from 'Styles/colors';

export default css({
  background: colors.primary,
  borderRadius: '50%',
  content: ' ',
  display: 'block',
  height: 8,
  position: 'absolute',
  right: 21,
  top: 23,
  width: 8,
});
