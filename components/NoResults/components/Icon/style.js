import cxs from 'cxs';

const magnifier = cxs({
  fill: 'currentColor',
});

const background = cxs({
  fill: '#fff',
});

const circle = cxs({
  fill: 'currentColor',
  opacity: 0.065,
});

export default {
  magnifier,
  background,
  circle,
};
