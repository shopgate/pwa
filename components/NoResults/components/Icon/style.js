import { css } from 'glamor';

const magnifier = css({
  fill: 'currentColor',
}).toString();

const background = css({
  fill: '#fff',
}).toString();

const circle = css({
  fill: 'currentColor',
  opacity: 0.065,
}).toString();

export default {
  magnifier,
  background,
  circle,
};
