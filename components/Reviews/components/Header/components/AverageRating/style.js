import { css } from 'glamor';

const container = css({
  alignItems: 'center',
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'space-between',
  marginBottom: 8,
}).toString();

export { container };
