import { css } from 'glamor';

const message = css({
  color: 'white',
  margin: 0,
  textAlign: 'left',
  maxWidth: '75%',
  paddingTop: 7,
  paddingBottom: 7,
}).toString();

export default {
  message,
};
