import { css } from 'glamor';
import colors from 'Styles/colors';

const label = css({
  background: colors.dark,
  opacity: 0.1,
  width: '100%',
  height: 16,
  marginBottom: 12,
}).toString();

export default {
  label,
};
