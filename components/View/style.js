import cxs from 'cxs';
import { light } from '../../styles/colors';

export default cxs({
  position: 'absolute',
  top: 0,
  left: 0,
  background: light,
  width: '100%',
  height: '100%',
  zIndex: 1,
});
