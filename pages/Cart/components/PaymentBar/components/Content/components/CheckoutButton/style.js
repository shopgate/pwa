import { css } from 'glamor';
import colors from 'Styles/colors';

const checkoutButton = css({
  background: colors.ctaPrimary,
  color: colors.ctaPrimaryContrast,
}).toString();

export default { checkoutButton };
