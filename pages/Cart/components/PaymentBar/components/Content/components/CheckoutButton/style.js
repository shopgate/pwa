import { css } from 'glamor';

const button = css({
  width: '100%',
}).toString();

const link = css({
  display: 'block',
  textAlign: 'center',
}).toString();

export default {
  button,
  link,
};
