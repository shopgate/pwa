import { css } from 'glamor';
import colors from 'Styles/colors';

export default css({
  color: colors.shade6,
  position: 'relative',
  display: 'inline-block',
  width: 'auto',
  zIndex: '1',
}).toString();
