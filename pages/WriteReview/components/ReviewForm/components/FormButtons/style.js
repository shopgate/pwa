import { css } from 'glamor';

/**
 * The style for the component element.
 * @type {string}
 */
const buttonLine = css({
  float: 'right',
  marginTop: '14',
}).toString();

export default {
  buttonLine,
};
